# Coding Standards: Data Validation

> [META-INSTRUCTION FOR AI AGENTS]
> Scope: Apply when defining data models, parsing network state, or importing configuration inputs.

## 1. Pydantic v2 Architecture

* Use Pydantic v2 (`pydantic>=2.0`) exclusively for data parsing and network input state verification.
